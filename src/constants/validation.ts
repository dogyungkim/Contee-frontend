/**
 * Validation constants for form inputs
 * Used to standardize validation rules across the application
 */

// Team validation
export const TEAM_VALIDATION = {
    NAME_MAX_LENGTH: 100,
    DESCRIPTION_MAX_LENGTH: 2000,
} as const;

// Add other validation constants as needed
