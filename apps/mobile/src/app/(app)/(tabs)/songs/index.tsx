import { ScreenPlaceholder } from '@/components/screen-placeholder'

export default function SongsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Read-only MVP"
      title="곡"
      description="팀 곡 라이브러리 read-only 화면을 연결할 자리입니다."
    />
  )
}
