import { ScreenPlaceholder } from '@/components/screen-placeholder'

export default function LoginScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Auth"
      title="로그인"
      description="모바일 OAuth 계약이 확정되기 전까지 개발용 우회 흐름만 연결합니다."
    />
  )
}
